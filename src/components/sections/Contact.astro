---
import { site } from '../../site.config';
---
<section id="contact">
  <div class="wrap">
    <h2>Contact</h2>
    <p>
      Email is best: <a href={`mailto:${site.email}`}>{site.email}</a>. For anything vLLM-related,
      Slack gets a faster answer.
    </p>
  </div>
</section>
