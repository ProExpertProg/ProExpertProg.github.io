// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // If you buy a domain later, change this and add a public/CNAME file.
  site: 'https://proexpertprog.github.io',
});
